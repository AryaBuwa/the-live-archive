name: Alive Archive - Keep Apps Alive

on:
  schedule:
    - cron: '0 */6 * * *'  # ping every 6 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping CafeMenu
        run: curl -s https://cafemenu.streamlit.app

      - name: Ping VibeScanAI
        run: curl -s https://vibescan.streamlit.app
